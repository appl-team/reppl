settings:
  logging:
    display:
      llm_cost: true
      # llm_cache: true
  concurrency:
    llm_max_workers: 100

# warning: You should not set API keys in this file, set them in `.env` file instead.
default_servers:
  default: gpt-4o-2024-11-20
